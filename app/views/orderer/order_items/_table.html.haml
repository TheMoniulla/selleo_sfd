%table.table.table-striped
  %tr
    %th Name
    %th Url
    %th Quantity
    %th User
    %th Actions
  - order_items.each do |order_item|
    %tr
      %td= order_item.product.name
      %td= link_to order_item.product.url, order_item.product.url, target: '_blank'
      %td= order_item.quantity
      %td= order_item.user.email
      %td= link_to 'ordered', orderer_order_item_path(order_item, cancel: order_item.order_id.present?), method: :put, class: 'btn btn-success'
